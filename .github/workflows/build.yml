name: Manual Gate

on:
  pull_request:
  workflow_dispatch:

jobs:
  manual-gate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "pending manual run"

  real-build:
    if: github.event_name == 'workflow_dispatch'
    needs: manual-gate
    runs-on: ubuntu-latest
    steps:
      - run: echo "real build here"
