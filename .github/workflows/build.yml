
name: build and upload2

on:
  pull_request:
  workflow_dispatch:

jobs: 
  firmware-build:
    runs-on: ubuntu-latest
    steps:
      - name: Summary and Download Instructions
        run: |
          echo "### Build Summary"
